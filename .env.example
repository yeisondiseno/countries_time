# Canonical production origin (metadata, sitemap, JSON-LD)
NEXT_PUBLIC_SITE_URL=https://www.countries-time.info

# Google AdSense — keep false until site approval; never show placeholders when false
NEXT_PUBLIC_ADS_ENABLED=false
